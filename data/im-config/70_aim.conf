IM_CONFIG_SHORT="$(gettext "activate AIM Framework")"

IM_CONFIG_LONG="$(gettext "AIM (Advanced Input Method) Framework
 * Required for all: aim
 * Language specific input conversion support:
   * Korean: aim-libhangul
   * Simplified Chinese: aim-sunpinyin
 * Application platform support:
   * GNOME/GTK+: aim-gtk
   * KDE/Qt: aim-qt")"

package_auto () {
:
}

package_menu () {
:
}
